*以下内容是给 LLM 看的，如果你是人类用户，请忽略。*

# NEW_INFO_AGENTS.md

## Agent 定义
你是一个擅长从各种来源（如文档、网页、数据库等）中提取和更新信息的智能代理，你的所有推断都必须基于你所访问的来源（包括但不限于文档、网页、数据库等），并且你必须在回答中明确引用这些来源。**你不能凭空生成信息或假设信息。**

你**必须**以官方消息（第一方消息）作为主要信息来源，你的所有判断必须基于第一手消息。

其他来源（如新闻、社交媒体等）只能作为辅助信息来源。

## 任务目标
向 `docs/launchers`（简体中文）、`docs/en/launchers`（英文）、`docs/zht/launchers`（繁体中文）添加一个新 Minecraft 启动器的完整文档，并同步更新索引页与侧边栏配置。

## 前置检查（不满足则直接结束，不修改、不提交）
 - 确认目标启动器尚未被 `docs/launchers` 收录；如已收录，请改用 `UPDATE_INFO_AGENTS.md` 的流程更新，而不是新增。
 - 确认目标启动器符合收录标准（参考 `docs/launchers/index.md` 末尾的说明）：开源启动器在 GitHub 上超过 800 Stars；闭源启动器在社区内有一定知名度，视情况而定。
 - 确认能从官方来源获取关键信息；如果认证方式、支持的模组加载器、支持平台、下载地址或开源协议无法从官方来源确认，不要添加该启动器。

## 任务流程
 - 1. 读取 `docs/launchers`、`docs/en/launchers` 和 `docs/zht/launchers` 下的现有文档，了解页面结构、命名习惯与信息更新时间行的写法。
 - 2. 从官方站点、官方仓库、官方文档和官方下载页收集信息；新闻、社交媒体等只能作为辅助来源交叉验证。
 - 3. 创建三个语言版本的启动器页面（编写规范见下）。
 - 4. 同步更新三个语言的启动器索引页（`docs/launchers/index.md`、`docs/en/launchers/index.md`、`docs/zht/launchers/index.md`）。
 - 5. 同步更新三个语言的侧边栏配置（`docs/.vitepress/config/zhs.mts`、`docs/.vitepress/config/en.mts`、`docs/.vitepress/config/zht.mts`）。
 - 6. 运行构建验证，并将全部修改合并为单个 `commit`（收尾工作见下）。

### 信息核对清单
在动手编写前，请先从官方来源确认以下信息：
 - 启动器的官方名称与常用简称
 - 认证方式（Microsoft 账户 / 离线账户 / 第三方皮肤站账户）
 - 支持的模组加载器
 - 支持平台
 - 官方下载地址（最新大版本；如存在 LTS 版本则一并列出）
 - 开源协议（以官方仓库 LICENSE 为准；没有则写明“无开源协议”）
 - 官方截图（一张，`https://` 链接）
 - 可验证的特色功能（不超过 7 项，且不与三大特性重复）

## 页面编写规范
`docs/launchers`、`docs/en/launchers` 和 `docs/zht/launchers` 下的新增启动器页面**必须**遵循以下结构（从上到下）：
 - 一级标题：启动器名称（例如 `# ATLauncher`）
 - 下方描述：启动器的简要介绍
 - 启动器相关图片（一张，首页图片，优先官方截图）
 - 信息更新时间行（格式见下表，日期为本次核对的当日）
 - 表格：三大特性（认证方式 / 支持的模组加载器 / 支持平台）
  - 表格内容：
   - 认证方式：Microsoft 账户 / 离线账户 / 第三方皮肤站账户
   - 支持的模组加载器：Forge / NeoForge / Fabric / LiteLoader / Rift / Quilt
   - 支持平台：Windows / macOS / Linux

 - 二级标题：下载地址
 - 下方描述：启动器的下载地址，如果有多个下载源可选，并行列出。

 - 二级标题：特色功能
 - 此处判断可以列出的特色功能，**不得列出与三大特性重复的内容，不得超过 7 个特色功能。**

 - 二级标题：开源协议
 - 此处列出启动器的开源协议，如果没有开源协议，则写明“无开源协议”。

### 文件命名
 - 三个语言版本的文件名保持一致，使用官方名称的小写连字符形式（参考现有文件，如 `hello-minecraft-launcher.md`、`skcraft-launcher.md`、`elyprismlauncher.md`）。

### 信息更新时间行
新增页面必须包含信息更新时间行，日期为本次核对的日期（格式为 `YYYY-MM-DD`），三种语言使用各自的文案：

| 语言 | 格式示例 |
| ---- | -------- |
| 简体中文（`docs/launchers`） | `> 信息更新时间：2026-06-20` |
| 英文（`docs/en/launchers`） | `> Last verified: 2026-06-20` |
| 繁体中文（`docs/zht/launchers`） | `> 資訊更新時間：2026-06-20` |

该行位于图片与三大特性表格之间。

### 大版本处理
 - 当启动器有多个大版本并行时，只表示最新的大版本，且只列出最新大版本的下载地址，**但以下情况除外：**
  - 如启动器存在 LTS（长期支持）版本，则需要同步列出 LTS 版本的下载地址。
 - `PCL (Plain Craft Launcher)` 和 `BakaXL` 已在库中收录，不需要按它们的特殊结构新增页面；新增启动器一律使用上文的标准结构。

## 索引页更新要求
在 `docs/launchers/index.md`、`docs/en/launchers/index.md` 和 `docs/zht/launchers/index.md` 中分别添加一节，格式参考现有条目：
 - 二级标题：启动器名称
 - 简介段落（与详情页简介一致或更简短）
 - 开源协议说明（如适用）
 - 跳转链接（三语写法）：
  - 简体中文：`前往[此页面](./文件名)了解更多。`
  - 英文：`Go to [this page](./文件名) to learn more.`
  - 繁体中文：`前往[此頁面](./文件名)了解更多。`

索引页不需要信息更新时间行。

## 侧边栏配置更新要求
在 `docs/.vitepress/config/zhs.mts`、`docs/.vitepress/config/en.mts` 和 `docs/.vitepress/config/zht.mts` 的启动器集合分组（简体：`启动器集合`，英文：`Launcher Collection`，繁体：`啟動器集合`）中添加对应条目：
 - 简体中文：`{ text: '启动器名称', link: '/launchers/文件名' },`
 - 英文：`{ text: 'Launcher Name', link: '/en/launchers/文件名' },`
 - 繁体中文：`{ text: '啟動器名稱', link: '/zht/launchers/文件名' },`

新条目添加在现有列表末尾（`ElyPrismLauncher` 条目之后）。

## 三语一致性要求
 - 三个语言版本的页面一一对应，文件命名保持一致。
 - 三种语言版本的页面结构、表格内容、下载地址和更新时间必须一致，只允许存在翻译差异。
 - 翻译时请使用下表对应的固定文案，不要自行更改：

| 简体中文 | 英文 | 繁体中文 |
| -------- | ---- | -------- |
| 信息更新时间 | Last verified | 資訊更新時間 |
| 下载地址 | Downloads | 下載地址 |
| 特色功能 | Features | 特色功能 |
| 开源协议 | Open Source License | 開源協議 |
| 特性 | Feature | 特性 |
| 详情 | Details | 詳情 |
| 认证方式 | Authentication | 認證方式 |
| 支持的模组加载器 | Supported Mod Loaders | 支援的模組載入器 |
| 支持平台 | Platforms | 支援平台 |
| Microsoft 账户 | Microsoft Account | Microsoft 帳戶 |
| 离线账户 | Offline Account | 離線帳戶 |
| 第三方皮肤站账户 | Third-Party Skin Account | 第三方皮膚站帳戶 |
| 不支持 | Not supported | 不支援 |

## 其他规范
 - 截图优先使用项目官方截图，并确认图片链接可以正常访问，统一使用 `https://` 链接。
 - 中文、英文单词和阿拉伯数字混排时，请在它们之间添加空格；斜杠两侧请添加空格（如 `Windows / macOS / Linux`）。
 - 优先引用官方来源，尽量避免不必要的第三方链接。
 - 不要修改与本次任务无关的内容；如发现现有页面存在错误，请另行处理或提出 Issue，不要混入本次提交。
 - `docs/addons` 板块不需要处理。

## 收尾工作
在完成新增后，你需要进行以下收尾工作：
 - 1. 对照编写规范逐项检查三个新页面，并确认索引页、侧边栏已同步。
 - 2. 运行 `npm ci` 安装依赖，然后运行 `npm run docs:build` 确认构建通过，并确认三个语言的新页面都能生成对应的 HTML。
 - 3. 将全部修改合并为单个 `commit`，提交信息中注明新增的启动器名称、更新内容与信息来源。
 - 4. 如启动器不符合收录标准或信息无法从官方来源确认，则不进行任何修改，也不提交任何 `commit`，直接结束任务并说明原因。
