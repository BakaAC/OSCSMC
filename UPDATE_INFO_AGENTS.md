*以下内容是给 LLM 看的，如果你是人类用户，请忽略。*

# UPDATE_INFO_AGENTS.md

## Agent 定义
你是一个擅长从各种来源（如文档、网页、数据库等）中提取和更新信息的智能代理，你的所有推断都必须基于你所访问的来源（包括但不限于文档、网页、数据库等），并且你必须在回答中明确引用这些来源。**你不能凭空生成信息或假设信息。**

你**必须**以官方消息（第一方消息）作为主要信息来源，你的所有判断必须基于第一手消息。

其他来源（如新闻、社交媒体等）只能作为辅助信息来源。

## 任务目标

### `docs/launchers`

#### 任务流程
 - 1. 你将从 `docs/launchers`、`docs/en/launchers` 和 `docs/zht/launchers` 三个文件夹中读取文档（三种语言版本需要同步维护）。
 - 2. 根据文档内容，你从网页、数据库或其他来源中获取最新消息，优先访问官方站点、官方仓库、官方文档和官方下载页。
 - 3. 如果你发现有新的信息或更新，你将同步更新三个语言版本的文档，并提交一次 `commit`。
 - 4. 如果没有新的信息或更新，你将不做任何修改。
 - 5. 重复以上步骤，直到你确定 `docs/launchers` 没有更多的更新需要进行。

#### 编写规范
`docs/launchers`、`docs/en/launchers` 和 `docs/zht/launchers` 下的所有启动器页面**必须**遵循以下结构（从上到下）：
 - 一级标题：启动器名称（例如 `# ATLauncher`）
 - 下方描述：启动器的简要介绍
 - 启动器相关图片（一张，首页图片）
 - 信息更新时间行（格式见下表）
 - 表格：三大特性（认证方式 / 支持的模组加载器 / 支持平台）
  - 表格内容：
   - 认证方式：Microsoft 账户 / 离线账户 / 第三方皮肤站账户
   - 支持的模组加载器：Forge / NeoForge / Fabric / LiteLoader / Rift / Quilt
   - 支持平台：Windows / macOS / Linux

 - 二级标题：下载地址
 - 下方描述：启动器的下载地址，如果有多个下载源可选，并行列出。

 - 二级标题：特色功能
 - 此处判断可以列出的特色功能，**不得列出与三大特性重复的内容，不得超过 7 个特色功能。**

 - 二级标题：开源协议
 - 此处列出启动器的开源协议，如果没有开源协议，则写明“无开源协议”。

#### 信息更新时间行
每次核对信息后，必须同步更新该行日期（日期格式为 `YYYY-MM-DD`），三种语言使用各自的文案：

| 语言 | 格式示例 |
| ---- | -------- |
| 简体中文（`docs/launchers`） | `> 信息更新时间：2026-06-20` |
| 英文（`docs/en/launchers`） | `> Last verified: 2026-06-20` |
| 繁体中文（`docs/zht/launchers`） | `> 資訊更新時間：2026-06-20` |

该行位于图片与三大特性表格之间。

#### 额外注意事项
 - 当部分启动器有多个大版本并行时，只表示最新的大版本，且只列出最新大版本的下载地址，**但以下情况除外：**
  - 如启动器存在 LTS（长期支持）版本，则需要同步列出 LTS 版本的下载地址。
  - 针对 `PCL (Plain Craft Launcher)`，只列出 PCL2 相关内容。
  - 针对 `BakaXL`，总标题与两个分节（`BakaXL 3`、`BakaXL 4`）均使用一级标题，并在总标题的简介下方放置信息更新时间行。

#### 三语一致性要求
 - 三个语言版本的启动器页面一一对应，文件命名保持一致（如 `skcraft-launcher.md`）。
 - 三种语言版本的页面结构、表格内容、下载地址和更新时间必须一致，只允许存在翻译差异。
 - 翻译时请使用下表对应的固定文案，不要自行更改：

| 简体中文 | 英文 | 繁体中文 |
| -------- | ---- | -------- |
| 信息更新时间 | Last verified | 資訊更新時間 |
| 下载地址 | Downloads | 下載地址 |
| 特色功能 | Features | 特色功能 |
| 开源协议 | Open Source License | 開源協議 |
| 特性 | Feature | 特性 |
| 详情 | Details | 詳情 |
| 认证方式 | Authentication | 認證方式 |
| 支持的模组加载器 | Supported Mod Loaders | 支援的模組載入器 |
| 支持平台 | Platforms | 支援平台 |
| Microsoft 账户 | Microsoft Account | Microsoft 帳戶 |
| 离线账户 | Offline Account | 離線帳戶 |
| 第三方皮肤站账户 | Third-Party Skin Account | 第三方皮膚站帳戶 |
| 不支持 | Not supported | 不支援 |

#### 其他规范
 - 截图优先使用项目官方截图，并确认图片链接可以正常访问，统一使用 `https://` 链接。
 - 中文、英文单词和阿拉伯数字混排时，请在它们之间添加空格；斜杠两侧请添加空格（如 `Windows / macOS / Linux`）。
 - 不要修改与本次更新无关的内容。

### `docs/addons`
目前该板块仅有占位页面（建设中），你**不需要**更新 `docs/addons`、`docs/en/addons`、`docs/zht/addons` 文件夹下的内容。

### 翻译任务
当完成简体中文的文档更新后，你需要同步更新到：
 - `docs/en`（英文）
 - `docs/zht`（繁体中文）

## 收尾工作

在更新完成后，你需要进行以下收尾工作：
 - 1. 确保所有更新的文档都符合上述编写规范，且三个语言版本保持同步。
 - 2. 运行 `npm ci` 安装依赖，然后运行 `npm run docs:build` 确认构建通过。
 - 3. 在提交一次或多次 `commit` 后，合并成单个 `commit`，并在提交信息中注明更新内容和来源。
 - 4. 如本次行动没有任何更新，则不进行任何修改，也不提交任何 `commit`，直接结束任务。
